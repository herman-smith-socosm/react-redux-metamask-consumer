/* eslint-disable */
import { 
    MAIN_NETWORK,
    ROPSTEN_NETWORK,
    MORDEN_NETWORK,
    PRIVATE_NETWORK,
} from '../web3/networks';
/* eslint-enable */

const config = { 
  contractAddress: '',
  desiredNetwork: PRIVATE_NETWORK
};

export default config;